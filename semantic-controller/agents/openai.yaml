interface:
  display_name: "Semantic Controller"
  short_description: "Create, bind, and verify ai_navigation semantic bases"
  default_prompt: "Use $semantic-controller to resolve this repo to its semantic base (or bootstrap one from zero via file-specs), produce a binding brief against the relevant architecture skill, and run integrity validation on the base."

policy:
  allow_implicit_invocation: true
