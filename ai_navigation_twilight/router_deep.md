# Router Deep

Use only when `router.md` does not resolve the task.

- Open one helper file first.
- Open no more than 2-3 source files before summarizing the owner.
- Search source when navigation misses; missing docs are not evidence of absence.
- For TGUI, do not continue without binding `$ss13-tgui`.
