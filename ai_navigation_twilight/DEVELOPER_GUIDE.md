# Developer Guide

This folder is a mental snapshot of `C:/Axis/Twilight-Axis` for agents. It is not source code and not proof that something exists.

Handoff pattern:

1. State the goal.
2. Tell the agent to use `$twilight-axis-ai-navigation`.
3. For TGUI/browser work, also tell it to use `$ss13-tgui`.
4. Require verification with live source before edits.
