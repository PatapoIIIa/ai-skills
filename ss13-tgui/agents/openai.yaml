interface:
  display_name: "SS13 TGUI"
  short_description: "Review and debug SS13 tgui interfaces, routes, and BYOND browser bridge code"
  default_prompt: "Review this SS13 TGUI-specific change only if it touches a tgui interface, route, frontend, backend ui_* proc, SStgui lifecycle, or BYOND browser bridge code. Check DM ui_interact/tgui_interact + ui_data/ui_static_data/ui_assets/ui_state/ui_act, route registration, React/Inferno + SCSS frontend, ByondUi/winset/winget/callByond usage, action security, JSON data shape, idiomatic codebase-native framework use, and overengineering. First confirm the repo's local conventions, then propose a focused fix plan that prefers deleting redundant machinery over adding it."
